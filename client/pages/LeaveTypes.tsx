import { useState } from "react";
import { ChevronDown, Pencil, Plus, Trash2, X } from "lucide-react";
import { TopBar } from "@/components/TopBar";

type LeaveType = {
  id: number;
  name: string;
  maxDays: number;
  carryForward: boolean;
  payType: "Paid" | "Unpaid";
  applicableTo: string;
};

const initialLeaveTypes: LeaveType[] = [
  { id: 1, name: "Annual Leave", maxDays: 24, carryForward: true, payType: "Paid", applicableTo: "All Employees" },
  { id: 2, name: "Sick Leave", maxDays: 12, carryForward: false, payType: "Paid", applicableTo: "All Employees" },
  { id: 3, name: "Compassionate Leave", maxDays: 5, carryForward: false, payType: "Paid", applicableTo: "All Employees" },
  { id: 4, name: "Compensatory Leave", maxDays: 10, carryForward: true, payType: "Paid", applicableTo: "All Employees" },
  { id: 5, name: "Maternity Leave", maxDays: 182, carryForward: false, payType: "Paid", applicableTo: "Female Employees" },
  { id: 6, name: "Manage Leave", maxDays: 10, carryForward: false, payType: "Unpaid", applicableTo: "All Employees" },
];

function SelectField({ value, onChange, children, placeholder }: { value: string; onChange: (value: string) => void; children: React.ReactNode; placeholder?: string }) {
  return <div className="relative"><select value={value} onChange={(event) => onChange(event.target.value)} className={`w-full appearance-none rounded-xl border border-slate-200 bg-white px-4 py-3 pr-10 text-base outline-none transition focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100 ${value ? "text-slate-800" : "text-slate-400"}`}><option value="">{placeholder}</option>{children}</select><ChevronDown className="pointer-events-none absolute right-4 top-3.5 h-5 w-5 text-slate-500" /></div>;
}

export default function LeaveTypes() {
  const [leaveTypes, setLeaveTypes] = useState(initialLeaveTypes);
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [existingType, setExistingType] = useState("");
  const [name, setName] = useState("");
  const [maxDays, setMaxDays] = useState("");
  const [carryForward, setCarryForward] = useState(false);
  const [payType, setPayType] = useState("Paid");
  const [applicableTo, setApplicableTo] = useState("All Employees");
  const [description, setDescription] = useState("");

  const closeModal = () => { setIsModalOpen(false); setExistingType(""); setName(""); setMaxDays(""); setCarryForward(false); setPayType("Paid"); setApplicableTo("All Employees"); setDescription(""); };
  const saveLeaveType = (event: React.FormEvent) => { event.preventDefault(); if (!name.trim() || !maxDays) return; setLeaveTypes((current) => [...current, { id: Date.now(), name: name.trim(), maxDays: Number(maxDays), carryForward, payType: payType as LeaveType["payType"], applicableTo }]); closeModal(); };

  return <div className="min-h-screen bg-[#f8fafc] text-slate-900"><TopBar /><main className="px-4 py-5 sm:px-6 sm:py-7 lg:px-8"><div className="mx-auto max-w-[1500px]"><div className="mb-5 flex flex-col gap-4 border-b border-slate-200 pb-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-bold tracking-tight sm:text-2xl">Leave Types</h2><p className="mt-1 text-sm text-slate-500">Manage leave policies for your employees</p></div><button type="button" onClick={() => setIsModalOpen(true)} className="inline-flex items-center justify-center gap-2 rounded-md bg-[#25c66d] px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:bg-[#1eaf5f]"><Plus className="h-4 w-4" />Add Leave Type</button></div><section className="overflow-hidden rounded-md border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[840px] text-sm"><thead className="bg-slate-50 text-left text-xs font-semibold text-slate-500"><tr><th className="px-4 py-3">Type Name</th><th className="px-4 py-3">Max Days</th><th className="px-4 py-3">Carry Forward</th><th className="px-4 py-3">Pay Type</th><th className="px-4 py-3">Applicable To</th><th className="px-4 py-3">Status</th><th className="px-4 py-3 text-center">Actions</th></tr></thead><tbody>{leaveTypes.map((type) => <tr key={type.id} className="border-t border-slate-100 text-slate-700 hover:bg-slate-50"><td className="px-4 py-3.5 font-medium text-slate-800">{type.name}</td><td className="px-4 py-3.5">{type.maxDays} Days</td><td className="px-4 py-3.5">{type.carryForward ? "Yes" : "No"}</td><td className="px-4 py-3.5">{type.payType}</td><td className="px-4 py-3.5">{type.applicableTo}</td><td className="px-4 py-3.5"><span className="rounded bg-emerald-50 px-2 py-1 text-xs font-medium text-emerald-700">Active</span></td><td className="px-4 py-3.5"><div className="flex justify-center gap-3"><button aria-label={`Edit ${type.name}`} className="text-slate-400 hover:text-emerald-600"><Pencil className="h-4 w-4" /></button><button aria-label={`Delete ${type.name}`} onClick={() => setLeaveTypes((current) => current.filter((item) => item.id !== type.id))} className="text-slate-400 hover:text-rose-600"><Trash2 className="h-4 w-4" /></button></div></td></tr>)}</tbody></table></div></section></div></main>{isModalOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/45 p-3 sm:p-6"><div className="absolute inset-0" onClick={closeModal} /><form onSubmit={saveLeaveType} className="relative max-h-[calc(100vh-1.5rem)] w-full max-w-[720px] overflow-y-auto rounded-2xl bg-white shadow-2xl"><header className="flex items-center justify-between border-b border-slate-200 px-6 py-6 sm:px-8"><h2 className="text-2xl font-bold tracking-tight">Add Leave Type</h2><button type="button" onClick={closeModal} aria-label="Close modal" className="rounded-full text-slate-500 hover:text-slate-800"><X className="h-7 w-7" /></button></header><div className="space-y-6 px-6 py-7 sm:px-8"><label className="block text-base font-semibold">Leave Type<SelectField value={existingType} onChange={setExistingType} placeholder="Select leave type">{leaveTypes.map((type) => <option key={type.id}>{type.name}</option>)}</SelectField></label><div className="flex items-center gap-4 text-center text-base font-semibold text-slate-800"><span className="h-px flex-1 bg-slate-200" />Or Create new Leave Type<span className="h-px flex-1 bg-slate-200" /></div><label className="block text-base font-semibold">Leave Type Name<input required value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Annual Leave" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 text-base font-normal outline-none placeholder:text-slate-400 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100" /></label><label className="block text-base font-semibold">Max Days Per Year<input required min="1" value={maxDays} onChange={(event) => setMaxDays(event.target.value)} type="number" placeholder="Enter number of days" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 text-base font-normal outline-none placeholder:text-slate-400 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100" /></label><div className="flex items-center justify-between gap-4"><div><p className="text-base font-semibold">Carry Forward</p><p className="mt-1 text-sm text-slate-500">Allow unused days to be used next year</p></div><button type="button" onClick={() => setCarryForward((value) => !value)} aria-pressed={carryForward} className={`relative h-8 w-14 rounded-full transition ${carryForward ? "bg-[#25c66d]" : "bg-slate-200"}`}><span className={`absolute top-1 h-6 w-6 rounded-full bg-white shadow transition ${carryForward ? "left-7" : "left-1"}`} /></button></div><div className="grid gap-5 sm:grid-cols-2"><label className="block text-base font-semibold">Pay Type<SelectField value={payType} onChange={setPayType}><option>Paid</option><option>Unpaid</option></SelectField></label><label className="block text-base font-semibold">Applicable To<SelectField value={applicableTo} onChange={setApplicableTo}><option>All Employees</option><option>Female Employees</option><option>Management</option></SelectField></label></div><label className="block text-base font-semibold">Description<textarea value={description} onChange={(event) => setDescription(event.target.value)} placeholder="Enter a brief description..." className="mt-2 min-h-[128px] w-full resize-y rounded-xl border border-slate-200 px-4 py-3 text-base font-normal outline-none placeholder:text-slate-400 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-100" /></label></div><footer className="flex justify-end gap-4 border-t border-slate-200 px-6 py-6 sm:px-8"><button type="button" onClick={closeModal} className="rounded-xl border border-slate-200 px-6 py-3 text-base font-semibold text-slate-600 hover:bg-slate-50">Cancel</button><button type="submit" className="rounded-xl bg-[#25c66d] px-6 py-3 text-base font-semibold text-white shadow-sm hover:bg-[#1eaf5f]">Save Leave Type</button></footer></form></div>}</div>;
}
