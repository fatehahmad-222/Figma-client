import { useMemo, useState } from "react";
import { Check, ChevronDown, Eye, EyeOff, Plus, Search, X } from "lucide-react";
import { useNavigate } from "react-router-dom";
import { TopBar } from "@/components/TopBar";

type Status = "Approved" | "Pending" | "Rejected";
type TimesheetRow = { id: number; employee: string; department: string; initials: string; avatar: string; week: string; regular: string; overtime: string; total: string; status: Status };
type Filters = { employee: string; department: string; month: string; year: string; store: string };

const rows: TimesheetRow[] = [
  { id: 1, employee: "Marcus Reed", department: "Engineering", initials: "MR", avatar: "bg-blue-100 text-blue-700", week: "Jul 01 - Jul 07", regular: "40 hrs", overtime: "8 hrs", total: "48 hrs", status: "Approved" },
  { id: 2, employee: "Jessica Lee", department: "HR", initials: "JL", avatar: "bg-pink-100 text-pink-700", week: "Jul 01 - Jul 07", regular: "38 hrs", overtime: "6 hrs", total: "44 hrs", status: "Pending" },
  { id: 3, employee: "Sarah Johnson", department: "Engineering", initials: "SJ", avatar: "bg-emerald-100 text-emerald-700", week: "Jul 01 - Jul 07", regular: "42 hrs", overtime: "12 hrs", total: "54 hrs", status: "Approved" },
  { id: 4, employee: "David Chen", department: "Finance", initials: "DC", avatar: "bg-amber-100 text-amber-700", week: "Jul 01 - Jul 07", regular: "35 hrs", overtime: "0 hrs", total: "35 hrs", status: "Rejected" },
  { id: 5, employee: "Emily Davis", department: "Marketing", initials: "ED", avatar: "bg-violet-100 text-violet-700", week: "Jul 01 - Jul 07", regular: "40 hrs", overtime: "4 hrs", total: "44 hrs", status: "Pending" },
  { id: 6, employee: "Robert Fox", department: "Operations", initials: "RF", avatar: "bg-orange-100 text-orange-700", week: "Jul 01 - Jul 07", regular: "45 hrs", overtime: "10 hrs", total: "55 hrs", status: "Approved" },
  { id: 7, employee: "Linda Park", department: "Design", initials: "LP", avatar: "bg-rose-100 text-rose-700", week: "Jul 01 - Jul 07", regular: "37 hrs", overtime: "2 hrs", total: "39 hrs", status: "Pending" },
  { id: 8, employee: "James Okafor", department: "Sales", initials: "JO", avatar: "bg-cyan-100 text-cyan-700", week: "Jul 01 - Jul 07", regular: "48 hrs", overtime: "5 hrs", total: "53 hrs", status: "Approved" },
];
const defaultFilters: Filters = { employee: "All Employees", department: "All Departments", month: "July", year: "2026", store: "All Stores" };
const statusStyles: Record<Status, string> = { Approved: "bg-emerald-50 text-emerald-600", Pending: "bg-amber-50 text-amber-600", Rejected: "bg-red-50 text-red-600" };

function FilterSelect({ label, value, onChange, children }: { label: string; value: string; onChange: (value: string) => void; children: React.ReactNode }) {
  return <label className="relative block text-xs font-medium text-slate-500">{label}<select value={value} onChange={(event) => onChange(event.target.value)} className="mt-1.5 w-full appearance-none rounded-lg border border-slate-200 bg-white px-3 py-2.5 pr-8 text-sm font-normal text-slate-700 outline-none focus:border-emerald-500">{children}</select><ChevronDown className="pointer-events-none absolute bottom-2.5 right-2.5 h-4 w-4 text-slate-400" /></label>;
}

export default function Timesheet() {
  const navigate = useNavigate();
  const [filtersVisible, setFiltersVisible] = useState(true);
  const [search, setSearch] = useState("");
  const [filters, setFilters] = useState(defaultFilters);
  const [applied, setApplied] = useState(false);
  const [addOpen, setAddOpen] = useState(false);
  const filteredRows = useMemo(() => rows.filter((row) => [row.employee, row.department].some((value) => value.toLowerCase().includes(search.toLowerCase()))), [search]);
  const update = (key: keyof Filters, value: string) => setFilters((current) => ({ ...current, [key]: value }));
  const apply = () => { setApplied(true); setFiltersVisible(false); };
  const reset = () => { setFilters(defaultFilters); setApplied(false); };
  const pending = rows.filter((row) => row.status === "Pending" || row.employee === "Robert Fox");

  return <div className="min-h-screen bg-slate-50 text-slate-800"><TopBar /><main className="mx-auto max-w-[1600px] px-4 py-5 sm:px-8"><div className="mb-5 flex flex-wrap items-center justify-between gap-4"><h1 className="text-2xl font-bold tracking-tight text-slate-900 sm:text-3xl">Timesheet</h1><button type="button" onClick={() => setAddOpen(true)} className="flex items-center gap-2 rounded-lg bg-emerald-500 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-emerald-600"><Plus className="h-4 w-4" />Add Timesheet</button></div><section className="mb-4 rounded-xl border border-slate-200 bg-white p-4 shadow-sm sm:p-5"><div className="flex flex-col gap-3 lg:flex-row"><div className="flex min-w-0 flex-1 items-center gap-2 rounded-lg border border-slate-200 px-3 py-2.5 focus-within:border-emerald-500 focus-within:ring-2 focus-within:ring-emerald-100"><Search className="h-4 w-4 shrink-0 text-slate-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search employees..." className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-slate-400" /></div><button type="button" className="rounded-lg bg-emerald-500 px-5 py-2.5 text-sm font-semibold text-white hover:bg-emerald-600">Search</button><button type="button" onClick={() => setFiltersVisible((value) => !value)} className="flex items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white px-4 py-2.5 text-sm font-medium text-slate-600 hover:bg-slate-50">{filtersVisible ? <EyeOff className="h-4 w-4" /> : <Eye className="h-4 w-4" />}{filtersVisible ? "Hide Filters" : "Show Filters"}</button></div>{filtersVisible && <div className="mt-5 border-t border-slate-100 pt-4"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5"><FilterSelect label="Employee" value={filters.employee} onChange={(value) => update("employee", value)}><option>All Employees</option>{rows.map((row) => <option key={row.id}>{row.employee}</option>)}</FilterSelect><FilterSelect label="Department" value={filters.department} onChange={(value) => update("department", value)}><option>All Departments</option>{[...new Set(rows.map((row) => row.department))].map((department) => <option key={department}>{department}</option>)}</FilterSelect><FilterSelect label="Month" value={filters.month} onChange={(value) => update("month", value)}>{["June", "July", "August"].map((month) => <option key={month}>{month}</option>)}</FilterSelect><FilterSelect label="Year" value={filters.year} onChange={(value) => update("year", value)}>{["2025", "2026", "2027"].map((year) => <option key={year}>{year}</option>)}</FilterSelect><FilterSelect label="Store" value={filters.store} onChange={(value) => update("store", value)}>{["All Stores", "Main Store", "City Store", "Airport Store"].map((store) => <option key={store}>{store}</option>)}</FilterSelect></div><div className="mt-4 flex flex-wrap items-center justify-end gap-4"><button type="button" onClick={apply} className="inline-flex items-center gap-2 rounded-lg bg-emerald-500 px-5 py-2.5 text-sm font-semibold text-white hover:bg-emerald-600">{applied && <Check className="h-4 w-4" />}Apply Filters</button><button type="button" onClick={reset} className="text-sm text-slate-500 hover:text-slate-800 hover:underline">Reset Filters</button></div></div>}</section><section className="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table className="w-full min-w-[900px] border-collapse text-xs"><thead><tr className="border-b border-slate-200 bg-slate-50 text-left text-[10px] font-bold uppercase text-slate-500"><th className="px-3 py-3">#</th><th className="px-3 py-3">Employee</th><th className="px-3 py-3">Week Period</th><th className="px-3 py-3">Regular</th><th className="px-3 py-3">Overtime</th><th className="px-3 py-3">Total</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Actions</th></tr></thead><tbody>{filteredRows.map((row) => <tr key={row.id} className="border-b border-slate-100 last:border-0 hover:bg-slate-50"><td className="px-3 py-3 text-slate-500">{row.id}</td><td className="px-3 py-3"><div className="flex items-center gap-2"><span className={`flex h-8 w-8 items-center justify-center rounded-full text-[10px] font-bold ${row.avatar}`}>{row.initials}</span><span><strong className="block text-slate-700">{row.employee}</strong><small className="text-[10px] text-slate-400">{row.department}</small></span></div></td><td className="px-3 py-3 text-slate-600">{row.week}</td><td className="px-3 py-3 text-slate-600">{row.regular}</td><td className="px-3 py-3 text-slate-600">{row.overtime}</td><td className="px-3 py-3 font-semibold text-slate-700">{row.total}</td><td className="px-3 py-3"><span className={`rounded px-2 py-1 text-[10px] font-semibold ${statusStyles[row.status]}`}>{row.status}</span></td><td className="px-3 py-3"><button type="button" onClick={() => navigate(`/timesheet/${row.id}`)} className="rounded-lg border border-emerald-400 px-3 py-1.5 font-medium text-emerald-600 hover:bg-emerald-50">View</button></td></tr>)}</tbody></table></section><section className="mt-6"><h2 className="mb-3 text-base font-bold text-slate-900">Pending Approval</h2><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{pending.map((row) => <button key={row.id} type="button" onClick={() => navigate(`/timesheet/${row.id}`)} className="flex items-center gap-3 rounded-xl border border-slate-200 bg-white p-3 text-left shadow-sm transition hover:border-emerald-400 hover:shadow-md"><span className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-full text-xs font-bold ${row.avatar}`}>{row.initials}</span><span className="min-w-0 flex-1"><strong className="block truncate text-sm text-slate-800">{row.employee}</strong><small className="block text-xs text-slate-500">{row.department}</small><small className="block text-xs text-slate-500">{row.total}</small></span><span className="rounded bg-amber-50 px-2 py-1 text-[10px] font-semibold text-amber-600">Pending</span></button>)}</div></section></main>{addOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><section role="dialog" aria-modal="true" className="relative w-full max-w-md rounded-2xl bg-white p-6 shadow-xl"><button type="button" onClick={() => setAddOpen(false)} aria-label="Close modal" className="absolute right-4 top-4 text-slate-400"><X className="h-5 w-5" /></button><h2 className="text-lg font-bold">Add Timesheet</h2><p className="mt-1 text-sm text-slate-500">Timesheet creation will be available here.</p><div className="mt-6 flex justify-end"><button type="button" onClick={() => setAddOpen(false)} className="rounded-lg bg-emerald-500 px-4 py-2 text-sm font-semibold text-white">Close</button></div></section></div>}</div>;
}
